<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/login_menu"
        android:icon="@drawable/login_icon"
        android:title="@string/login_icon"
        app:showAsAction="ifRoom"
        android:onClick="onClickLoginMenu"
        />

    <item
        android:id="@+id/signup_menu"
        android:title="@string/signup_menu"
        app:showAsAction="never"
        android:onClick="onClickSignUpMenu"
        />

    <item
        android:id="@+id/mypage_menu"
        android:title="@string/mypage_menu"
        app:showAsAction="never"
        android:onClick="onClickMyPageMenu"
        />

</menu>